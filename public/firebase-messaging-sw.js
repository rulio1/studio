
// This file is intentionally left blank.
// It's a placeholder for Firebase Cloud Messaging service worker.
// The actual SDK script is imported via workboxOptions in next.config.ts
self.addEventListener('fetch', function(event) {
  // This is a no-op fetch handler. 
  // It's required to make the app installable as a PWA in some browsers.
});
